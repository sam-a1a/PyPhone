# Runtime
pygame>=2.6.1,<3          # the phone UI: windowing, drawing, input
bcrypt>=4.1,<6            # password hashing (see apps/shared/security.py)
reportlab>=4.0,<5         # PDF export in the admin reports screen
openpyxl>=3.1,<4          # Excel export in the admin reports screen

# Test suite. Kept here so `pip install -r requirements.txt && pytest` is a
# single step in CI and on a fresh checkout.
pytest>=8.0
pytest-cov>=5.0
